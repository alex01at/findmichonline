# Safety net in case the webhost's document root ends up pointing here
# instead of the public/ folder. The document root should always be public/.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
