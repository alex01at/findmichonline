{% extends 'layout/base.twig' %}

{% block title %}Kartenlink – Deine digitale Visitenkarte{% endblock %}

{% block body %}
    <div class="card">
        <h1>{{ trans('home.heading') }}</h1>
        <p>{{ trans('home.subtitle') }}</p>
        <a class="btn" href="/register">{{ trans('home.cta_start') }}</a>
        <a class="muted-link" href="/login">{{ trans('home.cta_login') }}</a>
    </div>
{% endblock %}
