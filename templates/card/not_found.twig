{% extends 'layout/base.twig' %}

{% block title %}Karte nicht gefunden – Kartenlink{% endblock %}

{% block body %}
    <div class="card">
        <h1>{{ trans('card.not_found.heading') }}</h1>
        <p>{{ trans('card.not_found.text') }}</p>
        <a class="btn" href="/">{{ trans('card.not_found.home_link') }}</a>
    </div>
{% endblock %}
