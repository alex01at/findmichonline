{% extends 'layout/base.twig' %}

{% block title %}{{ trans('team.invite_invalid.heading') }} – findmichonline{% endblock %}

{% block body %}
    <div class="card">
        <h1>{{ trans('team.invite_invalid.heading') }}</h1>
        <p>{{ trans('team.invite_invalid.text') }}</p>
        <a class="btn" href="/login">{{ trans('auth.login.heading') }}</a>
    </div>
{% endblock %}
